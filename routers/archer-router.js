//POST and DELETE MVP
const { router } = require("../server");
const db = require("./archer-model");

router.post("/character", (req, res, next) => {});
router.delete("/character/:id", (req, res, next) => {});
module.exports = router;
